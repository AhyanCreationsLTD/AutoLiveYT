Name: AutoLiveYT Engine
on:
  repository_dispatch:
    types: [start_stream]

jobs:
  stream:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Install Tools
        run: |
          sudo apt update
          sudo apt install -y ffmpeg
          sudo curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp
          sudo chmod a+rx /usr/local/bin/yt-dlp

      - name: Start Live Stream
        run: |
          # ১. User-Agent যোগ করা হয়েছে ইউটিউবের বট প্রোটেকশন এড়াতে
          # ২. ভিডিও কোয়ালিটি এবং ফ্রেম রেট অপ্টিমাইজ করা হয়েছে
          # ৩. লো-ল্যাটেন্সি মোড সেট করা হয়েছে যাতে লাইভ স্মুথ চলে
          
          VIDEO_URL="${{ github.event.client_payload.url }}"
          USER_AGENT="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/119.0.0.0 Safari/537.36"
          
          DIRECT_URL=$(yt-dlp --user-agent "$USER_AGENT" -f "best" -g "$VIDEO_URL")
          
          ffmpeg -re -i "$DIRECT_URL" \
            -c:v libx264 -preset ultrafast -tune zerolatency \
            -b:v 3500k -maxrate 3500k -bufsize 7000k \
            -pix_fmt yuv420p -g 60 -r 30 \
            -c:a aac -b:a 128k -ar 44100 \
            -f flv "rtmp://a.rtmp.youtube.com/live2/${{ secrets.YOUTUBE_STREAM_KEY }}"
            
